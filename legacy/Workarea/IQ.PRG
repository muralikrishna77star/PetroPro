SELECT GROUP.GROUP_CODE, ITEM.ICODE, ITEM.IDESC, STOCK.SALES;
FROM BILL, ITEM, GROUP, STOCK;
WHERE ITEM.ICODE = BILL.ICODE;
.AND. GROUP.GROUP_CODE = ITEM.GROUP_CODE;
.AND. STOCK.ICODE = BILL.ICODE;
INTO TABLE SALEQUER.DBF
