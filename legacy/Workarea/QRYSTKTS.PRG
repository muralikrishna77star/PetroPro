SALEDATE={01/03/2008}

*SELECT A.ICODE, SUM(A.QTY) AS SALESQTY, SUM(B.SALES) AS STKSALES ;
FROM MARBIL08 A, MARSTK08 B WHERE A.BILL_DATE=B.SDATE AND B.SDATE=SALEDATE ;
AND A.ICODE=B.ICODE GROUP BY A.ICODE 

SELECT A.ICODE, A.QTY AS SALESQTY, B.SALES AS STKSALES ;
FROM MARBIL08 A, MARSTK08 B WHERE A.BILL_DATE=B.SDATE AND B.SDATE=SALEDATE ;
AND A.ICODE=B.ICODE 
