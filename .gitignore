# dependencies
node_modules/

# build output
dist/
.next/
out/
build/
*.tsbuildinfo

# runtime data
apps/api/data/

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
Thumbs.db
npm-debug.log*
coverage/

# redundant zip of legacy/Workarea (already tracked unpacked below)
PetBro.zip

# Real legacy DBF snapshot used for the FY2025-26 migration — contains real customer/vehicle
# data, must never be committed or uploaded to any build/deploy service.
WorkareaTill29Mar2026/
WorkareaTill29Mar2026.zip

# Vercel CLI project link (machine-local, per Vercel's own convention)
.vercel

# Original FoxPro install - local reference only, never pushed
legacy/Workarea/
